# Cursor IDE specific ignores

# Ignore all CSV files
*.csv

# Ignore LICENSE file
LICENSE

# Include reference to .gitignore to reuse its patterns
# (The patterns from .gitignore are automatically applied) 