name: mymodels
channels:
  - defaults
  - conda-forge
dependencies:
  - catboost=1.2.7
  - category_encoders=2.6.3
  - imbalanced-learn=0.13.0
  - ipykernel=6.29.5  # Required for Jupyter notebooks
  - ipython=8.30.0  # Required for Jupyter notebooks
  - jupyterlab  # JupyterLab UI
  - lightgbm=4.5.0
  - matplotlib-base=3.9.3
  - numpy=1.26.4
  - optuna=4.1.0
  - pandas=2.2.3
  - pip=24.3.1
  - py-xgboost=2.1.4
  # - pylint=3.3.5  # For development only
  # - pytest=8.3.4  # For development only
  - python=3.10.16
  - scikit-learn=1.5.2
  - scipy=1.14.1
  - setuptools=75.6.0
  - shap=0.46.0
  - wheel=0.45.1
  - seaborn=0.13.2
  # Removed packages (not used in codebase):
  # - plotly=5.24.1  # Only mentioned in comments, matplotlib is used instead
  # - python-graphviz=0.20.3  # No graphviz imports found in code
  # - tqdm=4.67.1  # Progress bars not used
  # - numba=0.60.0  # Not directly imported in code
